[project]
name = "sophnet-tts"
version = "0.1.0"
description = "Skill-local runtime dependencies for sophnet-tts"
requires-python = ">=3.10"
dependencies = [
  "requests>=2.28.0",
  "sophnet-tools>=0.0.1",
]
